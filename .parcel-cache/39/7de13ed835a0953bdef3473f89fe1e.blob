const images = require('./images/*.png');
console.log(images)
window.onload = () => {
  const bingoCard = document.getElementById('bingo-card')
  Array.from({length: 25}).map((_, i) =>{    
    // square.src = require(`img/bingo-${i + 1}.png`)
    bingoCard.appendChild(square)
    return square
  })
}