window.onload = () => {
  const bingoCard = document.getElementById('bingo-card')
  const squares = Array.from({length: 25}).map((_, i) =>{
    const square = document.createElement('div')
    square.innerText = i
    return square
  })

  bingoCard.appendChild(squares)
}